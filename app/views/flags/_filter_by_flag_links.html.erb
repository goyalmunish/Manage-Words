<% Flag.all.each do |flag| %>
    <%= link_to flag_name_and_value(flag), words_path(add_to_current_filters_and_orders(:flag_id => flag.id)), {:class => "btn btn-warning"} %>
<% end %>
<%= link_to 'No Flag', words_path(add_to_current_filters_and_orders(:flag_id => 0)), {:class => "btn btn-warning"} %>

