import { wordFlagCheckboxName } from 'frontend/helpers/word-flag-checkbox-name';
import { module, test } from 'qunit';

module('Unit | Helper | word flag checkbox name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = wordFlagCheckboxName([42]);
  assert.ok(result);
});
