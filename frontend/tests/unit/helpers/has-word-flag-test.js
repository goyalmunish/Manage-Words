import { hasWordFlag } from 'frontend/helpers/has-word-flag';
import { module, test } from 'qunit';

module('Unit | Helper | has word flag');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasWordFlag([42]);
  assert.ok(result);
});
