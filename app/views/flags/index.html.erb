<h2>Flags: </h2>

<table class="table table-bordered table-hover table-condensed">
  <thead>
    <tr>
      <!--<th>User</th>-->
      <th>Name - Value</th>
      <th>Desc</th>
      <!--<th colspan="3"></th>-->
    </tr>
  </thead>

  <tbody>
    <% @flags.each do |flag| %>
      <tr>
        <td><%= link_to "#{flag.name} - #{flag.value}", flag, {:class => "btn btn-default"} %></td>
        <td><%= flag.desc %></td>
        <!--<td><%= link_to 'Show', flag %></td>-->
        <!--<td><%= link_to 'Edit', edit_flag_path(flag) %></td>-->
        <!--<td><%= link_to 'Destroy', flag, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Flag', new_flag_path, {:class => "btn btn-primary"} %>
